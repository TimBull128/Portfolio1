@model IEnumerable<BB2020MVC.Models.Rules_Skills_Type>

<p>
    @Html.ActionLink("Create New", "AddSkillType")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditSkillType", new { id = item.ID }) <br />
                @Html.ActionLink("Delete", "DeleteSkillType", new { id = item.ID })
            </td>



        </tr>
    }

</table>
@Html.ActionLink("Create New", "AddSkillType")<br/>
@Html.ActionLink("Back To Landing", actionName: "Index", controllerName: "BaseRules");