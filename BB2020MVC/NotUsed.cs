using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace BB2020MVC
{
    public class NotUsed
    {
        /*
         private int GetNewRosterPlayerID()
         {
            var Players = GetAllRosteredPlayers();
            int i = 0;
            foreach(var item in Players)
            {
                if (item.RPlayerID != i)
                {
                    break;
                }
                i++;
            }
            return i;
         } 
         */

        ////string Response = "";
        ////foreach (var keys in FC.AllKeys)
        ////{
        ////    Response += "Key : " + keys + "<br/>");
        ////    Response += "Value: " + FC[keys] + "<br/>");
        ////}
        ////return View("Test", new { TstVal = Response);
        ///
    }
}